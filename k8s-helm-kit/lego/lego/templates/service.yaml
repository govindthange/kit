apiVersion: v1
kind: Service
metadata:
  name: {{ include "lego.fullname" . }}
  labels:
    {{- include "lego.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lego.selectorLabels" . | nindent 4 }}
