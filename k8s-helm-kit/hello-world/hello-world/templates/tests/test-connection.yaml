apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-world.fullname" . }} Tester"
  labels:
    {{- include "hello-world.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-world.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
